<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PH4N70M // Secure Terminal</title>
  <style>
    /* BASE TERMINAL STYLING */
    body {
      background: #000;
      color: #00ff00;
      font-family: "Courier New", monospace;
      margin: 0;
      padding: 0;
      overflow-x: hidden;
      scroll-behavior: smooth;
    }

    /* TERMINAL SCANLINE EFFECT */
    body::after {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(transparent 50%, rgba(0, 255, 0, 0.03) 50%);
      background-size: 100% 4px;
      pointer-events: none;
      z-index: 9999;
      animation: scanline 8s linear infinite;
    }

    @keyframes scanline {
      0% { background-position: 0 0; }
      100% { background-position: 0 100%; }
    }

    /* NOISE EFFECT */
    body::before {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 250 250' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
      opacity: 0.03;
      pointer-events: none;
      z-index: 9998;
    }

    /* SYSTEM TERMINAL HEADER */
    .terminal-header {
      background: #000;
      padding: 10px;
      border-bottom: 2px solid #0f0;
      text-align: center;
      position: sticky;
      top: 0;
      z-index: 1000;
      box-shadow: 0 0 20px #0f0;
    }

    .terminal-header h1 {
      margin: 0;
      font-size: 16px;
      text-shadow: 0 0 15px #0f0;
      animation: glitch 3s infinite;
    }

    @keyframes glitch {
      0%, 100% { text-shadow: 0 0 15px #0f0; }
      50% { text-shadow: 2px 2px 5px red, -2px -2px 5px blue; }
    }

    /* NAVIGATION - MOBILE FRIENDLY */
    nav {
      background: #000;
      padding: 10px 5px;
      border-bottom: 1px solid #0f0;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 10px;
      position: sticky;
      top: 50px;
      z-index: 1000;
    }

    nav a {
      color: #0f0;
      text-decoration: none;
      font-weight: bold;
      text-transform: uppercase;
      text-shadow: 0 0 5px #0f0;
      transition: color 0.3s;
      padding: 5px;
      border: 1px solid transparent;
      font-size: 12px;
    }

    nav a:hover, nav a.active {
      color: #fff;
      text-shadow: 0 0 10px #0f0;
      border: 1px solid #0f0;
      background: rgba(0, 255, 0, 0.1);
    }

    /* SECTIONS */
    section {
      padding: 20px 15px;
      border-bottom: 1px dashed #0f0;
      display: none;
    }

    section.active {
      display: block;
    }

    h3 {
      text-align: left;
      font-weight: bold;
      text-decoration: underline;
      text-shadow: 0 0 8px #0f0;
      overflow: hidden;
      white-space: nowrap;
      border-right: 2px solid #0f0;
      width: 0;
      animation: typing 2.5s steps(30, end) forwards, blink 0.7s infinite;
    }

    @keyframes typing {
      from { width: 0; }
      to { width: 100%; }
    }

    @keyframes blink {
      50% { border-color: transparent; }
    }

    /* CONTENT */
    .line {
      opacity: 0;
      transform: translateX(-20px);
      animation: fade 1s forwards;
    }

    @keyframes fade {
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    p, li, a {
      text-align: left;
      font-size: 14px;
      line-height: 1.4;
    }

    ul {
      list-style: square;
      padding-left: 20px;
    }

    a {
      color: #00ff00;
      text-decoration: none;
      font-weight: bold;
      text-shadow: 0 0 5px #0f0;
    }

    a:hover {
      color: #fff;
      text-shadow: 0 0 10px #0f0;
    }

    /* HACKER INFO BOXES */
    .hacker-info {
      background: rgba(0, 255, 0, 0.05);
      border: 1px solid #0f0;
      padding: 10px;
      margin: 10px 0;
      border-radius: 5px;
    }

    .hacker-info h4 {
      margin-top: 0;
      color: #0f0;
      text-shadow: 0 0 5px #0f0;
      font-size: 14px;
    }

    /* FOOTER */
    .footer {
      text-align: center;
      margin-top: 20px;
      font-size: 10px;
      color: #0f0;
      text-shadow: 0 0 5px #0f0;
      padding: 15px;
      border-top: 2px solid #0f0;
    }

    /* STATUS BAR */
    .status-bar {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      background: #000;
      border-top: 1px solid #0f0;
      padding: 5px 10px;
      font-size: 10px;
      display: flex;
      justify-content: space-between;
      z-index: 1000;
    }

    .status-bar span {
      text-shadow: 0 0 3px #0f0;
    }

    /* MOBILE SPECIFIC STYLES */
    @media (max-width: 768px) {
      .terminal-header h1 {
        font-size: 14px;
      }
      
      nav {
        top: 42px;
      }
      
      nav a {
        font-size: 10px;
        padding: 4px;
      }
      
      h3 {
        font-size: 16px;
      }
      
      p, li, a {
        font-size: 12px;
      }
      
      .status-bar {
        flex-direction: column;
        text-align: center;
        gap: 2px;
      }
    }
  </style>
</head>
<body>
  <!-- TERMINAL HEADER -->
  <div class="terminal-header">
    <h1>🖥️ PH4N70M TERMINAL v2.4.7 // ACCESS: ROOT</h1>
  </div>

  <!-- NAVIGATION -->
  <nav>
    <a href="#profile" class="nav-link active" data-section="profile">[PROFILE]</a>
    <a href="#skills" class="nav-link" data-section="skills">[SKILLS]</a>
    <a href="#about" class="nav-link" data-section="about">[ABOUT]</a>
    <a href="#projects" class="nav-link" data-section="projects">[PROJECTS]</a>
    <a href="#contact" class="nav-link" data-section="contact">[CONTACT]</a>
  </nav>

  <!-- PROFILE SECTION -->
  <section id="profile" class="active">
    <h3 class="type">[USER PROFILE]</h3>
    <div class="hacker-info">
      <h4>🦹 PHANTOM IDENTITY CONFIRMED</h4>
      <p>Alias: Phantom</p>
      <p>Clearance: Level 9</p>
      <p>Status: Active Operative</p>
    </div>
    <p class="line">> Name: Moseh Phantom</p>
    <p class="line">> Age: 20</p>
    <p class="line">> Career: Computer Science</p>
    <p class="line">> School: KINAP</p>
    <p class="line">> Specialization: Cyber Security</p>
  </section>

  <!-- SKILLS SECTION -->
  <section id="skills">
    <h3 class="type">[SKILLS DATABASE]</h3>
    <div class="hacker-info">
      <h4>⚡ CORE COMPETENCIES</h4>
      <p>Penetration Testing | Network Security | Cryptography</p>
    </div>
    <ul>
      <li class="line">[+] Website Creator</li>
      <li class="line">[+] Website Hosting</li>
      <li class="line">[+] App Creating</li>
      <li class="line">[+] App Launching</li>
      <li class="line">[+] HTML and JS Expert</li>
      <li class="line">[+] Web Design Specialist</li>
      <li class="line">[+] Network Penetration</li>
      <li class="line">[+] Security Analysis</li>
    </ul>
  </section>

  <!-- ABOUT SECTION -->
  <section id="about">
    <h3 class="type">[MISSION BRIEFING]</h3>
    <div class="hacker-info">
      <h4>🎯 ACTIVE OPERATIONS</h4>
      <p>Current Mission: Cyber Defense Training</p>
      <p>Status: Accepting New Operatives</p>
    </div>
    <p class="line">> 📘 Lessons Available Soon</p>
    <p class="line">> 💻 If Ready to Acquire software contact the admin: 
      <a href="mailto:mosesmulwa69@gmail.com">mosesmulwa69@gmail.com</a>
    </p>
    <p class="line">> 🔐 Security Protocols: Enabled</p>
  </section>

  <!-- PROJECTS SECTION -->
  <section id="projects">
    <h3 class="type">[ACTIVE PROJECTS]</h3>
    <div class="hacker-info">
      <h4>🚀 CLASSIFIED DEVELOPMENTS</h4>
      <p>All projects undergo security audit</p>
    </div>
    <ul>
      <li class="line">
        <a href="PhantomTrackerRemovere.html" target="_blank" download="PhantomTrackerRemovere.html">
          [PROJECT] Phantom Tracker Remover
        </a>
      </li>
      <li class="line">
        <a href="RemoverApk.html" target="_blank" download="RemoverApk.html">
          [PROJECT] Remover Apk
        </a>
      </li>
      <li class="line">
        <a href="TRACKER.html" target="_blank" download="TRACKER.html">
          [PROJECT] TRACKER
        </a>
      </li>
    </ul>
  </section>

  <!-- CONTACT SECTION -->
  <section id="contact">
    <h3 class="type">[SECURE COMMS]</h3>
    <div class="hacker-info">
      <h4>📡 ENCRYPTED CHANNELS</h4>
      <p>All communications are E2E encrypted</p>
    </div>
    <p class="line">> 📞 Secure Line: +254 113909834</p>
    <p class="line">> 📧 Encrypted Email: <a href="mailto:mosehphantom@gmail.com">mosehphantom@gmail.com</a></p>
    <p class="line">> 🌐 Darknet Presence: Available on request</p>
  </section>

  <!-- STATUS BAR -->
  <div class="status-bar">
    <span>USER: PHANTOM</span>
    <span>STATUS: ONLINE</span>
    <span>SECURITY: ENCRYPTED</span>
    <span>TIME: <span id="currentTime"></span></span>
  </div>

  <!-- FOOTER -->
  <div class="footer">
    © 2025 PH4N70M TECH // ALL SYSTEMS SECURE ⚡
  </div>

  <script>
    // Update current time
    function updateTime() {
      const now = new Date();
      document.getElementById('currentTime').textContent = now.toLocaleTimeString();
    }
    setInterval(updateTime, 1000);
    updateTime();

    // Section navigation
    document.querySelectorAll('.nav-link').forEach(link => {
      link.addEventListener('click', function(e) {
        e.preventDefault();
        
        // Remove active class from all links and sections
        document.querySelectorAll('.nav-link').forEach(l => l.classList.remove('active'));
        document.querySelectorAll('section').forEach(s => s.classList.remove('active'));
        
        // Add active class to clicked link
        this.classList.add('active');
        
        // Show corresponding section
        const sectionId = this.getAttribute('data-section');
        document.getElementById(sectionId).classList.add('active');
        
        // Animate the section content
        animateSection(sectionId);
      });
    });

    // Function to animate section content
    function animateSection(sectionId) {
      const section = document.getElementById(sectionId);
      let delay = 0.3;
      
      section.querySelectorAll(".type").forEach(el => {
        el.style.animation = "none";
        setTimeout(() => {
          el.style.animation = null;
        }, 10);
      });

      section.querySelectorAll(".line").forEach(el => {
        el.style.animation = "none";
        setTimeout(() => {
          el.style.animation = `fade 1s forwards`;
          el.style.animationDelay = `${delay}s`;
          delay += 0.3;
        }, 10);
      });
    }

    // Animate the initial active section
    animateSection('profile');

    // Add random glitch effects
    setInterval(() => {
      if (Math.random() > 0.8) {
        document.body.style.animation = 'glitch 0.3s';
        setTimeout(() => {
          document.body.style.animation = '';
        }, 300);
      }
    }, 10000);
  </script>
</body>
</html>
